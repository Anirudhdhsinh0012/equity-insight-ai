/**
 * NLP Ticker Service (Stub)
 * Minimal implementation to resolve build dependencies
 */

class NLPTickerService {
  // Placeholder implementation
  extractTickers(text: string): string[] {
    return [];
  }
}

export default NLPTickerService;
